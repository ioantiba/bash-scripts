##### Linux commands
- view all cron jobs owned by a specific user (replace $USER with specific user):
    - sudo crontab -l -u $USER
- edit user's crontab:
    - sudo crontab -e

##### Linux tools


##### Linux install steps